#include <iostream>
#include <memory> // manages dynamic memory
#include <vector>

int main () {
// unique= pointer that manages a raw pointer
std::unique_ptr<std::vector<std::shared_ptr<Test>>> vec_ptr; // vec pointer is an object



return 0;
}